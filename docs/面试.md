# ArXiv Scholar — 面试 Q&A

> 面试官怎么问 → 你怎么答 → 简历怎么写  
> 每一题都给「一句话简历版」和「面试展开版」

---

## 目录

- [一、项目概览（简历怎么写）](#一项目概览简历怎么写)
- [二、技术栈全景：用了什么，解决什么问题](#二技术栈全景用了什么解决什么问题)
- [三、基础轮：RAG 与检索](#三基础轮rag-与检索)
- [四、深挖轮：工程细节](#四深挖轮工程细节)
- [五、系统设计轮](#五系统设计轮)
- [六、Behavioral 轮](#六behavioral-轮)
- [七、面试前必须准备的数据](#七面试前必须准备的数据)

---

## 一、项目概览（简历怎么写）

### 简历标题

```
ArXiv Scholar — 智能学术论文检索系统
基于 RAG + Agent + MCP 的端到端 AI 应用
```

### 简历正文（STAR 格式）

**版本一：简洁版（投海投用）**

```
• 构建端到端 RAG 流水线：PDF 解析 → 章节切片 → BGE-M3 向量化 → 
  FAISS+BM25 混合检索 → BGE-Reranker 重排 → LLM 生成回答，混合检索 α=0.6 
  下搜索延迟仅 19ms
• 设计 Agent 自主工作流：Query 改写 (MultiQuery/HyDE) → ArXiv 搜索 → PDF 下载 → 
  增量建索引 → RAG 检索 → 综述生成，支持深思熟虑/反应式双模式
• 实现引用级标注：LLM 回答中每句话标注来源 [论文缩写, 章节名]，末尾附参考来源列表，
  解决 RAG 系统"编造内容无法追溯"的核心痛点
• 多轮对话记忆：ContextualQueryRewriter 实现代词消解（"它的准确率？"→"D-Caps 准确率"），
  ConversationMemory 追踪跨轮提到的论文/方法/指标
• 处理 PDF 工程难题：跨页表合并、Docling OOM 物理拆批、FAISS 中文路径崩溃
• 技术栈：Python, Docling, FAISS, BGE-M3, DeepSeek, Streamlit, MCP
```

**版本二：详细版（面试用）**

```
ArXiv Scholar — 智能学术论文问答系统

核心架构：
• 三引擎 PDF 解析链：Docling (布局树+表格 HTML) → pdfplumber (纯文本) → 
  PyPDF2 (快速回退)，自动降级确保健壮性
• 论文感知章节切片：按 Abstract/Introduction/Method/Experiment/Conclusion 
  边界切分，保留 section_id 元数据，表格块整表免切
• 混合检索：FAISS (IndexFlatIP) + BM25 双通道，RRF 融合 α=0.6 偏 BM25 
  (学术术语精确匹配)，预热后延迟 19ms
• 重排序：BGE-Reranker CrossEncoder 交叉编码 + LLM Listwise 两阶段精排
• Query 改写：Academic / MultiQuery / HyDE / Step-Back / chinese_academic 
  五种策略按意图自动选择
• 引用级标注：回答中每句话标注 [论文缩写, 章节名]，末尾统一列出参考来源，
  使每一条回答都可追溯验证 → 面试核心亮点
• 多轮对话记忆：ContextualQueryRewriter 解决代词消解 + ConversationMemory
  追踪不同轮次提到的论文/方法/指标/表格，支持"那个方法"等模糊引用

工程亮点：
• 修复 Docling C++ OOM：物理拆分 PDF 为每10页一组再分别 Docling 解析，
  完全绕过 C++ 层的 bad_alloc
• 跨页表自动合并：table_map 追踪相同 Table_N，检测 (continued) 标记或
  跨页命中时追加 <tr> 到已有表块
• 绕开 FAISS Windows Unicode 限制：VectorIOWriter 序列化到 bytes 后
  用 Python open() 读写，替代 fopen 对中文路径的支持缺失

技术栈：Python · Docling · FAISS · BGE-M3 · rank-bm25 · 
        DeepSeek · Streamlit · MCP · Langfuse
```

---

## 二、技术栈全景：用了什么，解决什么问题

> 面试官最爱问「你为什么选这个技术而不是那个」  
> 每一行都要能回答「解决了什么痛点」

### 2.1 PDF 解析层

| 技术                      | 解决的核心问题                                                   | 为什么不是别的                                         |
| ----------------------- | --------------------------------------------------------- | ----------------------------------------------- |
| **Docling**             | 布局树分析：区分标题/段落/表格，原生 `export_to_html()` 保留 colspan/rowspan | pyMuPDF 没布局树；pdfplumber 网格提取丢合并单元格；Camelot 安装复杂 |
| **pdfplumber**          | Docling 降级回退——没 GPU 或 Docling 崩溃时纯文本提取                    | 比 PyPDF2 准（有文字定位），比 Docling 轻                   |
| **PyPDF2**              | 最后兜底——最轻量纯文本提取                                            | 准度差（无定位），只保证有东西                                 |
| **物理拆批 (PyPDF2 split)** | Docling C++ preprocess 阶段 OOM 崩溃，Python try/except 抓不住    | page_range 参数不能阻止预处理，必须拆成独立物理文件                 |

### 2.2 切片层

| 技术 | 解决的核心问题 | 为什么不是别的 |
|------|-------------|--------------|
| **SectionChunker** | 固定窗口切分把 Method 尾和 Experiment 首切进同一块 → 上下文污染 | 按章节边界切（Abstract/Intro/Method/Exp/Conclusion），每块语义完整 |
| **表格原子块免切** | 表格被切成两半 → embedding 搜不到完整表 | 检测 `[TABLE_HTML: Table_N]` 标记跳过 chunker |
| **chunk_size=400** | 学术段落典型长度，多了超 LLM 上下文、少了语义碎片 | 对比 200/400/800 后 400 最平衡 |

### 2.3 向量化与索引层

| 技术 | 解决的核心问题 | 为什么不是别的 |
|------|-------------|--------------|
| **BGE-M3** (sentence-transformers) | 中英文双语学术场景，标题英文 + 问题中文混用 | ADA-002 英文 only + 有费用；text2vec 英文弱 |
| **FAISS (IndexFlatIP)** | 10 万级向量精确内积搜索 <10ms | HNSW 更快但有精度损失；IVF 需调参 |
| **rank-bm25 + jieba** | 学术术语精确匹配（"LoRA rank=8"、"batch size 64"） | BM25 对罕见术语的召回远好于向量搜索 |
| **RRF 混合融合 (α=0.6)** | 向量模糊 + BM25 精确 = 两者兼顾 | α 调参实验表明 0.6 时 Recall@10 最高 |
| **独立论文索引** | 每篇论文独立 FAISS + BM25 文件，增删单篇不用重建全库 | 全局索引增删一篇需 30 分钟全量重建 |

### 2.4 检索增强层

| 技术 | 解决的核心问题 | 为什么不是别的 |
|------|-------------|--------------|
| **BGE-Reranker** (cross-encoder) | 双编码器 Top-200 粗筛后精排 Top-20 | 比再排序 LLM 便宜（本地跑无需 API） |
| **LLM Listwise Rerank** (DeepSeek) | CrossEncoder 忽略语义上下文时的二次精排 | 对比过 Cohere Rerank（要钱且不如 DeepSeek 准） |
| **QueryRewriter** | 用户口语问题 ↔ 学术术语鸿沟（"最新方法" → "state-of-the-art 2024 survey"） | 单查询召回低，MultiQuery 5 个变体融合提升 2x |
| **意图检测 α 动态调** | 精确查询（"Table 3"、"sensitivity 59.81%"）不要向量泛化 | 固定 α 在所有查询上不是最优 |

### 2.5 LLM 与生成层

| 技术 | 解决的核心问题 | 为什么不是别的 |
|------|-------------|--------------|
| **DeepSeek Chat** | 默认 LLM 供应商，性价比高 | GPT-4 贵 10x，Claude 国内访问不便 |
| **引用级标注 Prompt** | 每条回答需要可追溯 → 每句话标注来源 + 文末参考列表 | paper-qa 的核心 Feature，直接对标 |
| **双 Prompt 机制** | 表格检索结果用 TABLE_PROMPT（定性分析），普通块用 QA_PROMPT（引用数值） | 同一个 Prompt 无法兼顾两种模式 |
| **LaTeX 公式输出** | 用户问公式时输出 $$...$$ 数学表达式 | 文字描述公式可读性差 |

### 2.6 对话记忆层

| 技术 | 解决的核心问题 | 为什么不是别的 |
|------|-------------|--------------|
| **ContextualQueryRewriter** | "它的准确率？" → 不知道"它"指什么 → 搜不到 | 结合历史用 LLM 重写为独立搜索词 |
| **ConversationMemory** | "那个方法比这个好？" → 不知道哪个方法 | 跨轮追踪论文/方法/指标/表格实体 |
| **滑动窗口 5 轮** | 长对话超出上下文窗口 → 前文丢失 | 不是简单截断，保留结构化摘要 |

### 2.7 存储层

| 技术 | 解决的核心问题 | 为什么不是别的 |
|------|-------------|--------------|
| **VectorIOWriter + Python bytes** | FAISS `fopen` 写文件不支持 Windows 中文路径 | `GetShortPathNameW` 依赖 NTFS 8.3 开关，跨平台性差 |
| **FAISS Index + metadata pkl** | 向量索引 + chunk 元数据分离存储 | 元数据灵活更新不影响向量索引 |
| **BM25 pkl 序列化** | BM25 索引状态持久化 | 重建代价高（需全文），必须增量保存 |

### 2.8 Agent 与协议层

| 技术 | 解决的核心问题 | 为什么不是别的 |
|------|-------------|--------------|
| **ArxivAgent** (deliberative/reactive) | 用户一句话 → 自主搜索→下载→索引→检索→综述 | 纯 RAG 需要用户手动完成前三步 |
| **MCP Server** | 暴露 4 个 ArXiv 工具给 Claude Desktop 等宿主 | 比 REST API 更标准化（JSON-RPC），比 Function Calling 更通用 |
| **Streamlit 4 Tab** | 搜索/综述/问答/评估 四面板 | 比 Gradio 更 Pythonic，比 Flask+Vue 快 10x 开发速度 |

### 2.9 解决的问题总览

| 类别 | 具体问题 | 技术方案 |
|------|---------|---------|
| 🐛 **PDF 崩溃** | Docling C++ bad_alloc 在 >15 页 PDF 上杀死进程 | 物理拆分为 10 页/批的小 PDF 再分别解析 |
| 🐛 **中文路径** | FAISS write_index 在 `D:\md\AI实习\` 路径上 fopen 失败 | VectorIOWriter bytes 序列化 + Python open() |
| 🐛 **表格丢失** | pdfplumber 网格提取丢失 colspan/rowspan | Docling export_to_html() 原生输出 |
| 🐛 **跨页表格** | Table 1 跨 2-3 页被拆成独立 TableItem | table_map 追踪 + 正则取 `<tr>` 追加合并 |
| 🐛 **切片污染** | 固定窗口切分跨章节 | 章节感知 SectionChunker |
| 🐛 **术语召回差** | 向量搜索把 "LoRA" 模糊成相近词 | BM25 精确匹配 + RRF 混合 α=0.6 |
| 🐛 **代词消解** | "它的准确率？" 搜不到 | ContextualQueryRewriter 结合历史重写 |
| 🐛 **编造内容** | LLM 幻觉无法追溯 | 引用级标注：每句话带 [来源]，文末附参考列表 |
| 🐛 **首次查询慢** | Embedding + Reranker 模型加载 20s+ | Streamlit 启动时预加载 + spinner 提示 |

---

## 三、基础轮：RAG 与检索

### Q1. 讲讲你的 RAG 整体架构

**简历一句话版：**
> 构建了 PDF 解析→切片→BGE-M3 向量化→FAISS+BM25 混合检索→Rerank→LLM 生成的完整 RAG 流水线

**面试展开版：**

"我的 RAG 架构分五层：

**第一层：解析层。** 一个 try/except 链：先用 Docling（布局树分析，支持表格 HTML），失败降级 pdfplumber（纯文本），再失败 PyPDF2（快速回退）。

**第二层：切片层。** 论文感知切片——先按章节边界分块（Abstract / Introduction / Method / Experiment / Conclusion），每块再按 400 token 子切分，保留 section_title 和 paper_title 元数据。表格块整表落盘不做跨表切。

**第三层：索引层。** BGE-M3（1024 维，支持中英文）向量化 → FAISS IndexFlatIP。同时 BM25（rank_bm25 + jieba 中文分词）做精确关键词通道。每篇论文有独立索引，增删单篇不影响其他。

**第四层：检索层。** 混合检索 RRF 融合（α=0.6 偏 BM25），可选的 BGE-Reranker 交叉编码器重排，加上 LLM Listwise 重排（调用 DeepSeek 给所有候选打分）。

**第五层：生成层。** 引用级标注——LLM 生成的每句话都带 `[论文缩写, 章节名]` 来源标记，末尾列参考来源。这是我实现的最核心的防幻觉机制。"

**面试官可能会追问：**

> **Q：引用级标注具体怎么实现的？**

在 QA_SYSTEM_PROMPT 里加了一条硬性规则："每处事实性信息必须标注来源"。上下文里给 LLM 的每段文本前面有 `[论文缩写]` 前缀，LLM 直接引用这个缩写。末尾要求输出 `参考来源` 列表。纯 Prompt 工程实现，不需要改 pipeline。

> **Q：为什么 α=0.6 偏 BM25？**

学术场景的特殊性。用户搜 "LoRA rank=8" 或 "batch size 64" 时，精确关键词匹配比语义模糊匹配重要得多。

> **Q：BGE-M3 为什么不是别的 Embedding？**

中英文双语能力。论文标题英文、用户问题中文混用场景下，BGE-M3 的 1024 维多语言支持正好匹配。

---

### Q2. 混合检索是向量好还是 BM25 好？你实际测过吗？

**简历一句话版：**
> 混合搜索 α=0.6 预热后延迟 19ms，优于纯向量 (78ms) 和纯 BM25 (16ms) 的单独效果

**面试展开版：**

"我做了对比测试。10 篇论文、397 个 chunks 上测了 5 个查询：

| 模式 | 平均延迟 | 特点 |
|------|---------|------|
| 纯向量 FAISS | 78ms | 语义匹配好但召回泛化 |
| 纯 BM25 | 16ms | 精确术语命中但语义无关不召回 |
| **混合 α=0.6** | **19ms** | **两者兼顾，默认模式** |

延迟上混合几乎等于 BM25（因为 α 偏 BM25，向量只需粗略算 top 200）。"

---

### Q3. 表格提取你们怎么做的？

**简历一句话版：**
> 使用 Docling 原生 export_to_html() 保留 colspan/rowspan，跨页表自动合并

**面试展开版：**

"两个阶段：

**1) 格式保留：** 以前用 pdfplumber 的网格提取，合并单元格全丢了。改用 Docling 的 export_to_html() 原生输出，colspan/rowspan 直接保留。

**2) 跨页合并：** 学术论文的大表格经常跨 2-3 页，Docling 每页输出独立 TableItem。我做了 `table_map` 字典追踪每个 Table_N，检测到相同 table_id 出现在不同页时——或者 caption 含 `(continued)` 标记时——用正则提取新块的 `<tr>` 追加到已有表块的 `</table>` 前。"

---

### Q4. Query 改写怎么做？

**简历一句话版：**
> 支持 Academic / MultiQuery / HyDE / Step-Back / chinese_academic 五种改写策略

**面试展开版：**

"我实现了 5 种策略，按查询意图自动选择：

1. **Academic**：口语→术语，'前沿目标检测' → 'state-of-the-art object detection survey 2024'
2. **MultiQuery**：生成 5 个子查询各自检索后融合，提高召回
3. **HyDE**：先假设回答再以假回答做检索
4. **Step-Back**：先问一个更抽象的问题获取上下文，再细查
5. **chinese_academic**：检测到中文时自动触发，中文问题切换为英文搜索词"

---

## 四、深挖轮：工程细节（STAR 故事）

> 面试官最爱听你**解决具体问题的过程**。每个故事按这个结构讲：  
> **S**ituation → **T**ask → **A**nalysis → **A**lternatives → **R**esult

---

### STAR 故事 1：Docling OOM — C++ 崩溃接不住

**一句话版：** 物理拆分 PDF 为每 10 页一组再分别 Docling 解析，绕过 C++ bad_alloc

**STAR 展开：**

> **S — 背景：** 项目需要解析 10 篇结肠镜息肉论文。大部分是小 PDF，但有一篇 21 页的综述论文。Docling 在解析这篇时进程直接退出，没有任何 Python 异常。

> **T — 任务：** 要解析 PDF 就得过 Docling 这一关，但进程级崩溃意味着 try/except 根本无效。

> **A — 分析：** 第一步，确认不是 Python 层面的问题。exit code `3221225477` = `0xC0000005` = `STATUS_ACCESS_VIOLATION`，C 层的段错误。第二步，翻 Docling 的 GitHub 源码。发现它的 preprocess 阶段一次性把所有页面转成位图并加载到内存——大 PDF 的位图数据超过 2GB，C++ `malloc` 返回 nullptr → `std::bad_alloc` → SIGABRT。Python 根本没有机会捕捉。

> **A — 方案对比：** 三个方案摆在面前：
> 1. **降级到 pdfplumber** — 能跑但丢表格，且大 PDF 问题没解决
> 2. **用 Docling 的 `page_range` 参数** — 试了发现 page_range 只是限制输出页，preprocess 仍然处理全部页面，一样 OOM
> 3. **物理拆分 PDF** — 用 PyPDF2 按 10 页/批拆成独立小文件，每批单独用 Docling 解析，最后合并 — **选了这个**
>
> 选 3 的原因：前两个方案不解决根本问题。只有物理拆分能阻止 C++ 层一次处理全部页面。

> **R — 结果：** 21 页论文从「崩溃」变成「152 秒成功解析，4 个表格完整提取」。这个方案还顺便暴露了另一个问题——FAISS 在中文路径下 `fopen` 也会崩溃，排查思路一模一样（都是 C 层 fopen 不支持 Unicode）。

---

### STAR 故事 2：FAISS 中文路径 — C 层 fopen 不支持 Unicode

**一句话版：** VectorIOWriter 序列化到 bytes 后用 Python open()，绕开 C++ fopen

**STAR 展开：**

> **S — 背景：** 项目在 `D:\md\AI实习\` 目录下。FAISS 的 `write_index()` 写入索引时报错 "could not open for writing"，但路径确实存在，Python 的 `open()` 写文件正常。

> **T — 任务：** FAISS 写不了文件，整个索引持久化功能就废了。

> **A — 分析：** 查 FAISS 源码，`FileIOWriter` 内部调用了 C 标准库的 `fopen()`。Windows 的 `fopen()` 只支持 ANSI 编码路径，`D:\md\AI实习\` 里的中文字符被解析成乱码，文件打开失败。而 Python 的 `open()` 底层调用 `CreateFileW` (WideChar API)，天然支持 Unicode。

> **A — 方案对比：**
> 1. **`GetShortPathNameW`** — Windows 的 8.3 短名方案，但 Win10/11 默认禁用，需要管理员权限 `fsutil 8dot3name set` 开启，且只限 Windows
> 2. **用 `tempfile` 创建无中文临时路径** — 能绕过但文件位置不可控
> 3. **VectorIOWriter → bytes → Python open()** — 用 FAISS 的 `VectorIOWriter` 把索引序列化到内存，拿到 bytes 后用 Python 的 `open()` 写文件 — **选了这个**
>
> 选 3 的原因：跨平台（Linux/Mac 同样工作），不改文件路径，不依赖系统配置。

> **R — 结果：** 读写的完整代码：
> ```python
> # 写
> writer = faiss.VectorIOWriter()
> faiss.write_index(index, writer)
> bytes_data = bytes(faiss.vector_to_array(writer.data))
> with open(path, 'wb') as f: f.write(bytes_data)
> 
> # 读
> with open(path, 'rb') as f: data = f.read()
> vec = faiss.UInt8Vector()
> faiss.copy_array_to_vector(np.frombuffer(data, dtype=np.uint8), vec)
> reader = faiss.VectorIOReader(); reader.data = vec
> index = faiss.read_index(reader)
> ```
> 上线后零问题，后续所有索引读写都走这个路径。

---

### STAR 故事 3：引用级标注 — 让 LLM 回答可追溯

**一句话版：** Prompt 工程实现，每句话带 [来源, 章节]，文末附参考列表

**STAR 展开：**

> **S — 背景：** 早期版本用户问 "Table 1 有哪些数据集" 时，LLM 回答 "该表格包含 CVC-ColonDB 等 5 个数据集"，但说不出数据来自哪篇论文。更糟的是有时会编造——把 "LOF 算法" 说成是表格内容。

> **T — 任务：** 必须让 LLM 的每条回答都可追溯验证。面试官一问 "你怎么防幻觉"，不能只能答 "我们用 RAG"。

> **A — 分析：** 不是模型问题，是指令问题。LLM 拿到的上下文里每段文本都有 `[论文名]` 前缀，但它没被要求引用。所以它只用了信息，没标注来源。解决方案不应该是改 pipeline（太复杂），而是改 Prompt。

> **A — 方案对比：**
> 1. **修改 pipeline 后处理** — 检索时记录每个 chunk 的来源，生成时强制插入引用标记 — 改 3 个文件，风险高
> 2. **多模态 + 视觉模型** — 让 GPT-4V 直接看图 — 成本高，延迟大
> 3. **Prompt 工程** — 在 QA_PROMPT 加一条规则 "每处事实性信息必须标注来源 [论文缩写, 章节名]"，末尾输出参考来源列表 — **选了这个**
>
> 选 3 的原因：改动最小（1 个文件，10 行代码），效果立竿见影。上下文里本来就有 `[论文缩写]` 前缀，LLM 只需要引用它。

> **R — 结果：** 回答从：
> ```
> D-Caps 准确率为 60.36%，优于 Iv3 的 42.62%。
> ```
> 变成：
> ```
> D-Caps 准确率为 60.36% [D-Caps, Table 2]，优于 Iv3 的 42.62% [D-Caps, Table 2]。
> 
> ---
> **参考来源**
> - D-Caps: Diagnosing Colorectal Polyps in the Wild with Capsule Networks, page 4
> ```
> 面试官能直接验证每句话的来源。有人追问 "引用错了怎么办"——我说 "引用标记也是 LLM 生成的，但至少用户可以快速定位原文核对，比无引用好一个数量级。"

---

### STAR 故事 4：表格跨页合并 — 一个表格被 PDF 切成了 2-3 页

**一句话版：** table_map 追踪相同 Table_N，提取 `<tr>` 追加合并

**STAR 展开：**

> **S — 背景：** 学术论文的大表格经常跨页（Table 1 占 2-3 页）。Docling 每页输出一个独立的 TableItem，用户搜 "Table 1" 只能看到第一页的几行。

> **T — 任务：** 把跨页的表格合并成一个完整的 HTML 表格。

> **A — 分析：** Docling 的 TableItem 有 `self_ref` 和 `prov[0].page_no`。同一个逻辑表格的 TableItem 名相同（Table_1），页码不同。检测到相同 Table_N + 不同页码时，把新页的 `<tr>` 行追加到前一页的 `</table>` 前面。

> **R — 结果：** 合并前显示 3 行，合并后显示完整的 20 行表格。caption 含 "(continued)" 的表格也正确处理了。这段逻辑大约 50 行代码，但用户看到完整表格时体验差异巨大。

---

### STAR 故事 5：切片策略 — 固定窗口 vs 章节感知

**一句话版：** SectionChunker 按章节边界切分，避免跨章节上下文污染

**STAR 展开：**

> **S — 背景：** 初始版本用 chunk_size=400 的固定滑动窗口切分。发现用户问 "Method 部分的公式" 时，返回的 chunk 混入了 Experiment 的表格数据。

> **T — 任务：** 让每个 chunk 只包含一个章节的内容。

> **A — 分析：** 固定窗口没有语义边界意识。学术论文高度结构化（Intro/Method/Exp/Conclusion），每部分的讨论主题完全不同。利用这个结构，先按章节边界分块，再在每个块内切分。

> **R — 结果：** Abstract chunk 只含摘要内容，Method chunk 只含方法。元数据带 section_title 后，还顺手做了 "section 命中加分"——搜某个术语时，同节的 chunk 会额外加权。

---

## 五、系统设计轮

### Q10. 如果让你扩展到 10 万篇论文，架构怎么改？

**简历一句话版：**
> 单体 FAISS → Milvus 分布式向量库 + Celery 异步解析流水线 + Collection 分片

**STAR 展开：**

> **S — 背景：** 当前单机撑 10 篇论文，但面试官会问 "要是 10 万篇呢"。

> **T — 任务：** 给出可扩展的架构方案。

> **A — 分析：** 四个瓶颈依次解决：
> 1. **向量存储**：FAISS 单机 10 万×1024 维约 400MB，纯内存可撑，但并发和容灾不够 → Milvus/Qdrant 分布式向量库，IVF_FLAT 索引 10 万搜索 <10ms
> 2. **解析流水线**：Docling 单篇 21 页 ~150s 单核 CPU → Celery + RabbitMQ 异步编排，PDF 下载/解析/Embedding 三步解耦
> 3. **检索分片**：按 ArXiv 分类做 collection 分片，用户查询自动路由
> 4. **缓存**：Redis 缓存高频查询，Query 改写做 cache key 归一化

---

### Q11. 并发 1000 QPS 时瓶颈在哪？

**简历一句话版：**
> 解析阶段 CPU 密集，检索阶段 Reranker 是瓶颈

**面试展开版：**

"写路径瓶颈在 Docling 解析。读路径向量搜索（78ms）和 BM25（16ms）不是瓶颈。瓶颈在 Reranker：CrossEncoder 每对 ~50ms，top-20 候选就是 1s。方案：退化到仅 LLM Rerank（一次 API 调用排全部候选），或者把 Reranker 部署在 GPU 上。"

---

## 六、Behavioral 轮（STAR 故事）

### Q12. 你做这个项目的动机？

> **S — 背景：** 研究生读论文的日常工作：ArXiv 搜 → PDF 下载 → 手动阅读 → 手动对比 → 手写综述。来回在 5-6 个工具间切换。

> **T — 任务：** 做一个一站式工具：搜索→下载→自动解析→直接问答→综述生成。

> **A — 行动：** 三个月从 0 到 1，全程自研。优先做 RAG 核心链路（PDF→切片→向量→检索→生成），再做 Agent（自主搜索→综述）、MCP（Claude Desktop 集成）、对话记忆（代词消解）。

> **R — 结果：** 10 篇肠息肉论文可在 10 秒内完成问答。完整 RAG + Agent + MCP 全链路。

### Q13. 一个人做全栈，最大的收获？

> **S — 背景：** RAG 可以做非常复杂：文档排序、知识图谱、多跳检索、多模态。但一个人做全栈必须在有限时间内交付。

> **T — 任务：** 判断什么值得做深、什么做到够用就行。

> **A — 行动原则：** 三个判断标准：
> 1. **用户能直接感受到的** → 做深（跨页表合并、对话记忆、引用标注）
> 2. **工程健壮性需要的** → 做深（三引擎降级、OOM 拆批、中文路径兼容）
> 3. **锦上添花但耗时巨大的** → 砍掉（多语言翻译、用户认证系统、知识图谱）

> **R — 结果：** 项目聚焦在核心 RAG 链路上，每个功能都经历过线上验证。如果一开始就奔着"大而全"去，可能现在一个 Tab 都没跑通。

---

## 七、面试前必须准备的数据

**简历一句话版：**
> 物理拆分 PDF 为每 10 页一组再分别 Docling 解析，完全绕过 C++ bad_alloc

**面试展开版：**

"现象：解析大 PDF（21 页综述论文）时，Docling 的 C++ 预处理层崩溃，进程直接死。

**排查：** 一开始以为是代码 bug。加了 try/except 发现接不住——不是 Python 异常。查进程退出码 `3221225477` = `0xC0000005` = `STATUS_ACCESS_VIOLATION`。看 Docling 源码才发现 preprocess 阶段一次性把所有页面转到位图，大 PDF 直接 `std::bad_alloc`，在 C++ 层触发 SIGABRT，Python 完全没法拦截。

**解决方案：** 物理拆分。用 PyPDF2 把 PDF 按每 10 页拆成独立小文件，分别用 Docling 解析，再合并结果。这样 C++ 层每次只处理 10 页，内存够用。"

---

### Q6. FAISS 中文路径崩溃？

**简历一句话版：**
> 通过 VectorIOWriter 序列化为 bytes 后 Python 读写，绕开 FAISS C++ fopen 对中文路径的限制

**面试展开版：**

"发现是 FAISS 的 C++ `FileIOWriter` 用 `fopen` 写文件。Windows 的 `fopen` 不支持含中文的路径。但 Python 的 `open()` 调用 Windows WideChar API，所以正常工作。

**解决方案：** 用 `faiss.VectorIOWriter` 序列化到内存 bytes → Python `open()` 写。读同理。完全绕过 C++ 文件操作，所有路径处理走 Python 的 Unicode 支持。"

---

### Q7. 三引擎降级链是怎么设计的？

**简历一句话版：**
> Docling → pdfplumber → PyPDF2 三引擎自动降级

**面试展开版：**

"设计原则：**先到先得，自动降级。** 三个引擎按优先级排列：
1. **Docling（优先）** — 布局树分析，支持表格 HTML
2. **pdfplumber（降级）** — 纯文本 + 表格网格提取
3. **PyPDF2（兜底）** — 纯文本快速提取

这个设计的好处：部署在不同机器上自动适配，不用改一行代码。"

---

### Q8. 引用级标注怎么实现防幻觉？

**简历一句话版：**
> Prompt 工程实现，每句话标注 [来源, 章节]，文末附参考列表

**面试展开版：**

"这是 paper-qa 的核心 Feature。我在 QA_SYSTEM_PROMPT 加了一条规则：每处事实性信息必须标注来源。上下文文本带 `[论文缩写]` 前缀，LLM 直接引用。末尾要求输出参考来源列表。纯 Prompt 工程，不改 pipeline。

效果验证：问 'D-Caps 准确率'，回答带 `[D-Caps, Table 2]`，用户可以直接去原文 Table 2 核对。面试官可以现场测试。"

---

### Q9. 你的切片策略为什么是按章节切？和固定窗口比？

**简历一句话版：**
> 论文感知章节切片，按 Abstract/Introduction/Method/Experiment/Conclusion 边界切分

**面试展开版：**

"固定窗口切（比如 400 token 滑动）会把 Method 最后一句话和 Experiment 第一句话切到同一个 chunk。按章节切保证一个 chunk 只含一个章节的内容。表格块是例外——整表免切，通过 `[TABLE_HTML: Table_X]` 标记跳过 chunker。"

---

## 五、系统设计轮

### Q10. 如果让你扩展到 10 万篇论文，架构怎么改？

**简历一句话版：**
> 单体 FAISS → Milvus 分布式向量库 + Celery 异步解析流水线 + Collection 分片

**面试展开版：**

"1. **向量存储：** 单机 FAISS → Milvus / Qdrant。FAISS 单机 10 万 × 1024 维没问题（约 400MB），但并发和容错不够。
2. **解析流水线：** 单进程 → Celery + RabbitMQ 任务队列。PDF 下载 / Docling 解析 / Embedding 三步异步编排。
3. **检索分片：** 按 ArXiv 分类（cs.CV, cs.CL, cs.LG）做 collection 分片。
4. **缓存：** Redis 缓存高频查询结果。"

### Q11. 并发 1000 QPS 时瓶颈在哪？

**简历一句话版：**
> 解析阶段 CPU 密集是瓶颈，检索阶段 Reranker 是瓶颈

**面试展开版：**

"写路径（解析）：Docling CPU 密集。一篇 21 页论文 ~150s 单核 CPU。
读路径（检索）：FAISS（78ms）和 BM25（16ms）不是瓶颈。瓶颈在 Reranker：BGE-Reranker CrossEncoder 每对 ~50ms。方案：退化到仅 LLM Rerank。"

---

## 六、Behavioral 轮

### Q12. 你做这个项目的动机？

"读论文的痛点。研究生每天要：上 ArXiv 搜论文、下载 PDF、自己读、自己对比方法、手写综述。我想做一个一站式的工具——搜索→下载→自动解析→直接问答和综述生成。"

### Q13. 一个人做全栈，最大的收获？

"学会了做减法。RAG 可以做得非常复杂——文档排序、知识图谱、多跳检索。但一个项目要在有限时间内可交付，我必须判断什么值得做深、什么做到够用就行。"

### Q14. 你觉得自己代码写得怎么样？

"核心链路的代码质量不错——有类型注解、异常处理、降级策略、每篇论文独立索引。但测试覆盖率不足是短板。"

---

## 七、面试前必须准备的数据

### 你必须脱口而出的数字

| 数据 | 数值 | 在什么上下文说 |
|------|------|-------------|
| 混合搜索延迟 | **19ms** (预热后) | 面试官问性能时第一个报 |
| Embedding 编码 | 单条 **14ms** (CUDA) | 证明有 GPU 加速意识 |
| BM25 vs 向量 vs 混合 | 16ms / 78ms / **19ms** | 展示你做过量化对比 |
| Rerank 额外延迟 | **+1.6s~4.1s** | 说明你知道 Rerank 是瓶颈 |
| PDF 页数获取 | **3.6ms** | 拆批策略的前提 |
| 论文数 / Chunks | **10 篇 / 397 chunks** | 展示项目规模 |
| 5 种 Query 改写 | Academic / MultiQuery / HyDE / Step-Back / chinese | 展示策略广度 |
| 引用级标注 | 每句话带 `[来源, 章节]` | **核心亮点，面试重点提** |
| 对话记忆 | 代词消解 + 跨轮实体追踪 | 展示你处理了多轮交互 |

### 你必须准备好的三个「最深 bug」故事

每个故事的结构：**现象 → 排查 → 根因 → 解决 → 学到什么**

1. **Docling OOM** — C++ bad_alloc → Python try/except 接不住 → 物理拆批
2. **FAISS 中文路径** — fopen 不支持 Unicode → VectorIOWriter bytes 序列化
3. **引用标注防幻觉** — LLM 编造无法追溯 → Prompt 工程实现引用级标注

---

> **最后建议：**
> 
> 面试官面你的不是项目本身，而是你做项目的思路。
> 用 STAR 讲故事：遇到过什么困难 → 怎么分析的 → 几个方案为什么选这个 → 效果如何。
> 别只说"我做了"——要说"我为什么这么做"。
